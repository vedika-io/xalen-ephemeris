[package]
name = "xalen-ayanamsa"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "40+ ayanamsa (sidereal precession) systems for XALEN Ephemeris"
keywords = ["ayanamsa", "sidereal", "precession", "lahiri", "vedic"]
categories = ["science", "mathematics"]

[dependencies]
xalen-time = { version = "0.6.0", path = "../xalen-time" }
xalen-coords = { version = "0.6.0", path = "../xalen-coords" }
# True Chitra anchors Spica from the commercial-OK anchors crate, NOT the
# NC-gated fixed-star catalog — so the ayanamsa no longer depends on xalen-stars.
xalen-star-anchors = { version = "0.6.0", path = "../xalen-star-anchors" }
serde.workspace = true

[lints]
workspace = true
